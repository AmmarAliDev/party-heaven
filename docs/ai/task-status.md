# Task Status

## Current Milestone

Light-only theme conversion completed on 2026-08-31 (scope: remove dark theme support and make the light theme constant on every device regardless of the OS/device color preference; implementation: deleted the `next-themes` dependency and its `ThemeProvider` (`src/components/providers/theme-provider.tsx`), `ThemeToggle` (`src/components/theme-toggle.tsx`), `src/config/theme.ts`, and the `AppTheme` type in `src/types/app.ts`; removed `ThemeProvider` from the root layout plus the `suppressHydrationWarning` attributes that existed only for theme switching; `AppToaster` now renders Sonner in a fixed `theme="light"` with static action/cancel button colors; removed the `.dark` token block, the `@custom-variant dark` directive, and `.dark ::selection` from `src/app/globals.css`, and stripped remaining `dark:` Tailwind variants from shared UI components (`alert`, `dropdown-menu`, `field`) and checkout success copy; removed the `ThemeToggle` from the admin shell and the disabled toggle comments in the storefront header/mobile nav; tests: deleted `tests/components/providers/theme-provider.test.tsx`, updated `tests/components/providers/app-toaster.test.tsx` to assert fixed light rendering, and updated `tests/smoke/ui-foundation.test.ts` to check only the light palette (and that `.dark` is absent); docs updated in `docs/dev/ui-conventions.md`, `docs/dev/setup.md`, `docs/dev/testing.md`, `docs/ai/implemented-features.md`, `docs/ai/task-status.md`, `docs/ai/testing-status.md`, and `docs/ai/coding-conventions.md`).

Mobile header rework + hamburger-hosted user menu completed on 2026-08-30 (scope: the mobile header is now a three-zone layout — hamburger drawer trigger on the far left, centered logo, and right-side actions with the cart rightmost; there is NO separate user-menu button in the mobile header — the hamburger drawer (`StorefrontMobileNav`) hosts the full menu with the same options as the desktop user menu: page links (Home/About/Blog/Contact), `Admin Panel` (admins), `Your Orders` (signed-in), plus wishlist/account/sign-out actions; `StorefrontHeaderAuthControls` modes are back to `"mobile"` (drawer) and `"desktop"` (dropdown) and now passes `isAdmin` to the drawer; the mobile search trigger stays in the header but is hidden for now (`hidden` wrapper in `src/components/layout/app-header.tsx`, re-enable by removing it); the mobile logo is narrower (`h-8 w-40 md:h-10 md:w-60 lg:h-20 lg:w-70`) so the centered layout fits small screens, and the header row uses equal side columns (`grid-cols-[minmax(0,1fr)_auto_minmax(0,1fr)]`) so the logo stays truly centered; the mobile drawer no longer renders the catalog category list (category browsing on mobile remains reachable via the bottom nav **Collections** action at `/categories`), and the now-unused `categoryMenuItems`/`categoryMenuError`/`cartHref` props were removed along with the `buildStorefrontCategoryMenu` usage in `AppHeader`; `<UserMenu />` (desktop) and the mobile drawer both gained a `Your Orders` item (`/account/orders`) before `Sign out` for signed-in users; tests: updated `tests/components/layout/user-menu.test.tsx` (Your Orders present for signed-in, hidden for signed-out), updated `tests/features/auth/sign-out-ui.test.ts` for the simplified `StorefrontMobileNav` props, and added `tests/components/layout/storefront-mobile-nav.test.tsx` (page links, wishlist/account actions, Your Orders for signed-in, Admin Panel for admins, categories absent); docs updated in `docs/dev/ui-conventions.md`, `docs/dev/architecture.md`, `docs/dev/testing.md`, `docs/ai/implemented-features.md`, `docs/ai/task-status.md`).

Homepage featured-products fetch limit raised to 5 completed on 2026-08-30 (the featured-products carousel renders 5-up on `2xl`, but the service resolved only `HOMEPAGE_FEATURED_PRODUCTS_LIMIT = 4` products, leaving the `2xl` row one card short; `HOMEPAGE_FEATURED_PRODUCTS_LIMIT` bumped from 4 to 5 in `src/features/homepage/featured-products.ts` so the section always fetches 5 products — the 5th fills the `2xl` row and simply scrolls into view on smaller viewports, with no client-side breakpoint logic; the most-sold → recent-published → stored-fallback resolution order and the `HOMEPAGE_CAROUSEL_MAX_ITEMS` (8) render cap are unchanged; updated homepage service tests for the new limit — top-5 most-sold hydration, sparse-sales fallback filling 5 cards, and recent-before-fallback backfill filling 5 slots — and added the featured-products hydration contract to `docs/dev/homepage-section-contract.md`).

Homepage carousel 2xl density fix completed on 2026-08-29 (featured products — and the shared `party-heaven` carousel plus `featured-categories` — now render 5 visible cards on `2xl` (≥ 1536 px) instead of 4; root cause: `HOMEPAGE_CAROUSEL_ITEM_CLASS` used the Tailwind v3 important prefix form `2xl:!basis-1/5`, which Tailwind v4 silently ignores, so `xl:basis-1/4` persisted at `2xl`; fix: switched to the v4 suffix form `2xl:basis-1/5!`, and `xl:basis-1/4!` / `2xl:basis-1/5!` in `FEATURED_CATEGORIES_CAROUSEL_ITEM_CLASS`; the responsive ladder for all homepage product carousels is now 1 default / 2 sm / 3 md / 4 xl / 5 2xl; updated the config doc comments, homepage carousel unit tests, and the UI conventions + homepage section contract docs).

Storefront footer redesign completed on 2026-08-28 (scope: replace the old three-section footer — brand blurb, company/policy pill links, newsletter placeholder — with a four-column layout; desktop renders **Quick Links / Help / Policies / Contact** as four columns (`md:grid-cols-4`), and mobile collapses each column behind its heading as four rows with a chevron toggle; implementation: new client `FooterColumn` (`src/components/layout/footer-column.tsx`) that starts collapsed on mobile (content `hidden md:block`, chevron button `md:hidden` with `aria-expanded`/`aria-controls` and rotate-on-open) and always renders expanded at the `md` breakpoint; `AppFooter` is now an async server component that loads live published catalog categories via `getCatalogCategories()` (same source/ordering as the header, with `FOOTER_CATEGORY_NAV_LOAD_FAILED` error logging and graceful empty fallback) and renders up to `NAVBAR_DIRECT_CATEGORY_LIMIT` = 6 quick-link categories (Party Heaven first, then alphabetical) via `buildStorefrontNavbarCategoryMenu` plus a `View All` button to `/categories`; Help links About us/Contact us/Your Orders (`/about`, `/contact`, `/account/orders`); Policies links Privacy Policy/Refund Policy/Shipping Policy/Terms of Service (`/privacy`, `/return-policy`, `/shipping-policy`, `/terms`); Contact shows Email/Phone placeholder rows (blank for now); the brand blurb, newsletter placeholder block, and the footer's `footerPreviewLink`/`footerNewsletterPlaceholder` guarded-surface usages were removed (config entries left intact); tests: added `tests/components/layout/app-footer.test.tsx` (four headings, quick-link cap + ordering + View All href, Help/Policy hrefs, Contact placeholders, mobile collapse/expand toggle with aria state, desktop `md:block` visibility); docs updated in `docs/dev/ui-conventions.md`, `docs/ai/task-status.md`, `docs/ai/implemented-features.md`). Styling follow-up on 2026-08-29: column headings are now responsive (`text-sm md:text-xl`), column links use the muted footer palette (`text-muted hover:text-muted-foreground`), and the `View All` action is a ghost text-link (`buttonVariants({ variant: "ghost" })` with `hover:bg-transparent`/`p-0!`) instead of an outlined button; the footer tests now assert the responsive heading classes, muted link classes, and the ghost link styling.

Variant cover images on listing cards and admin edit form completed on 2026-08-28 (scope: multi-variant products whose images are all stored per-variant showed only on the PDP — `Product.images` only returns product-level rows, so product cards fell back to the placeholder gradient and the admin product edit form showed an empty image list; implementation: (1) storefront — added `mergeVariantImagesIntoProducts` in `src/server/db/catalog-queries.ts`, a single batched `productImage.findMany` per listing that appends each product's variant images (ordered by variant order — default variant first — then `position`) after its product-level images, and applied it to `listPublishedProductsByCategory`, `listAllPublishedProducts`, `getRelatedPublishedProducts`, `listPublishedProductsByIds`, and `searchPublishedProducts` so category grids, search results, related-product cards, and homepage sections all show the real variant cover with no N+1 per product; `mapProductToCard` passes the same variant order to `mapProductImages` so the DEFAULT variant's image is used as the card cover when no product-level image exists; (2) admin — `getAdminProductById` now fetches variant-level images scoped to the product and merges them into the form record so the edit form restores per-variant image rows (correct `variantIndex`) on load; (3) cleanup — the homepage `backfillVariantPrimaryImages` workaround (and its `variantImage` fallback param in `toFeaturedProductItem`) became redundant and was removed; tests: added listing merge coverage in `tests/server/db/catalog-queries.test.ts` (default-variant-first ordering, category cards, batch grouping, product-level-first ordering, empty/no-product short-circuits), card cover coverage in `tests/features/catalog/service.test.ts` (variant-only cover used; product-level preferred; default variant preferred over a lower-positioned non-default variant), and admin edit-form coverage in `tests/features/admin/products/service.test.ts` (variant images surface with correct `variantIndex`, null-product path skips the image query); docs updated in `docs/dev/architecture.md`, `docs/ai/implemented-features.md`, `docs/ai/architecture-decisions.md` (Decision 11), and this file).

Variant-specific product images completed on 2026-08-28 (scope: let admins attach each product image to a specific variant for variant products, and make the storefront gallery variant-aware; the `ProductImage.productVariantId` column already existed in the schema/migrations so no new migration was required; implementation: (1) admin — `adminProductImageSchema` gained an optional `variantIndex` (index into the product's `variants` array, validated in-range when variants are enabled), the product server action reads `imageVariantIndex` rows, `AdminProductFormRecord.images` now carry `variantIndex`, `mapAdminProduct` resolves `productVariantId → variantIndex`, and `createImages`/`createVariants`/`upsertVariants` were reworked so variants are created/upserted BEFORE images (returning ordered variant ids) and images are attached to a variant (`productVariantId`) or left product-level (`productId`) with the DB CHECK constraint keeping rows valid; the update flow now clears both product-level and variant-level images before recreating; the admin product form shows a per-image variant select (with an "All variants (shared)" option) when variant mode is enabled; (2) storefront — `storefrontProductSelect.images` includes `productVariantId`, `ProductImage` gained `variantId`/`variantLabel`, `getProductBySlug` maps variant images grouped by variant order with per-variant primary flags, and the new `ProductOverview` client wrapper (`src/features/catalog/components/product-overview.tsx`, selection helpers in `src/features/catalog/lib/variant-selection.ts`) owns the PDP variant-selection state keeping `ProductImageGallery` + `ProductPanel` in sync — tapping a variant-specific thumbnail selects that variant and the picker switches the gallery to that variant's images; (3) robustness — homepage featured products and deal-spotlight campaigns fall back to the first variant's image when a product has no product-level image, and cart thumbnails already prefer variant images; tests updated in `tests/features/catalog/product-detail.test.ts`, `tests/features/catalog/product-image-gallery.test.tsx`, `tests/features/admin/products/service.test.ts`, `tests/features/admin/products/validation.test.ts`, and `tests/features/admin/products/admin-product-form.test.tsx`; docs updated in `docs/dev/architecture.md`, `docs/dev/domain-model.md`, `docs/dev/admin-guide.md`, `docs/dev/ui-conventions.md`, `docs/ai/data-model.md`, `docs/ai/implemented-features.md`, and `docs/ai/task-status.md`).

Variant images follow-up fixes completed on 2026-08-28 (found by manual browser reproduction): (1) admin save for "All variants (shared)" images failed the whole product save because the zod preprocess turned the select's `null` into `undefined`, which the inner `z.number().nullable()` schema rejected in Zod 4 — `parseVariantIndexish` now returns `null` for both `null` input and empty string (regression test added); (2) the PDP gallery never showed variant images because Prisma's `Product.images` relation only returns product-level rows — `_getPublishedProductBySlugImpl` now fetches variant-level images scoped to the product's variants and merges them (test added in `tests/server/db/catalog-queries.test.ts`); (3) products whose variants have no `options` JSON got no storefront picker, so `buildVariantGroups` now groups such variants under a generic `Variant` group using the variant title (test added in `tests/features/catalog/product-detail.test.ts`); (4) storefront image URLs from hosts not allowlisted in `next.config.ts` crashed the PDP with a next/image "unconfigured host" error — new `src/config/image-hosts.ts` is the single source of truth (used by both `next.config.ts` remotePatterns and `normalizeCatalogImageUrl`), and `normalizeCatalogImageUrl` now rejects unconfigured hosts so pages fall back to the placeholder instead of crashing (new `tests/features/catalog/product-image-url.test.ts`); verified end-to-end in the browser that variant images save, persist after reload, render per-variant in the gallery, and switch via both the picker and thumbnails).

Robust storefront catalog search completed on 2026-08-21 (scope: fix search so it matches category names, plurals, and multi-word queries, and ranks results by relevance instead of raw creation date; root causes: `searchPublishedProducts` matched only `name`/`shortDescription`/`description` with the whole query string — no category-name matching, no tokenization (so `"scented candle"` missed products whose names contained only one word), no plural widening (`"chains"` never matched `Gold Chain`, `"candles"` never matched `Candle Trio`), and `createdAt DESC` ordering let incidental description hits (e.g. balloons mentioning candles) crowd out literal name/category matches within the small result limit; fix: added `src/features/catalog/lib/search-text.ts` (`tokenizeSearchQuery`, `expandSearchToken` with ies/es/s plural variants, `expandSearchQuery`, `tokenMatchesText`), `searchPublishedProducts` now tokenizes the query, widens plural forms, and ORs across `name`/`shortDescription`/`description`/`category.name` while fetching a candidate pool (limit×4, clamped 24–60) so ranking has room, and `dbCatalogSearchAdapter` now scores candidates (name 100 > category 70 > shortDescription 40 > description 20 per matching token) and slices to the final limit; searching a category name now surfaces every product under it, `"chains"` matches `chain` products, and `"candles"` shows candle products over balloons; tests: added `tests/features/catalog/search-text.test.ts` (tokenizer/stemmer/matcher), extended `tests/features/catalog/search-service.test.ts` (category matching, plural queries, multi-word, name-over-description ranking), extended `tests/server/db/catalog-queries.test.ts` (where-clause widening for category names, plural tokens, multi-word OR, publish-state filters, candidate pool sizes, empty-query short circuit); docs updated in `docs/dev/search-architecture.md`, `docs/dev/architecture.md`, `docs/ai/architecture-decisions.md` (Decision 10), `docs/dev/testing.md`, `docs/ai/implemented-features.md`, and `docs/ai/task-status.md`).

Mobile bottom nav + silent cart toasts + deals-section gating completed on 2026-08-20 (scope: (1) the homepage `party-heaven` "Party Heaven deals" section is now hidden entirely when there are no active deals — `PartyHeavenSectionBlock` renders `null` when its hydrated product list is empty instead of showing the old "No Party Heaven deals right now" empty state, so the homepage never advertises a deals section with nothing to offer; (2) cart success toasts removed app-wide — `CartItemQuantityControls` no longer calls `notify.success` on add/update/remove (the shared `runMutation` success toast and its now-unused `successMessage` param were removed), matching the already-silent PDP `ProductAddToCart` / `ProductCardAddToCart`; only error toasts remain; (3) new mobile-only fixed bottom navigation bar `MobileBottomNav` (`src/components/layout/mobile-bottom-nav.tsx`) with five icon-above-label actions — Collections (`/categories`), Search (opens shared search command dialog), Cart (opens shared cart drawer with live item-count badge), Home (`/`), and Profile (`/account/profile`) — mounted in `src/app/(storefront)/layout.tsx` and the root homepage `src/app/page.tsx`, with active-route `aria-current` highlighting, `z-40` (below drawer/dialog `z-50`), `md:hidden`, and `AppFooter` gaining `pb-24 md:pb-0` so footer content clears the fixed bar; tests: added `tests/components/layout/mobile-bottom-nav.test.tsx` (renders 5 actions, hrefs, search/cart open shared overlays, count badge, empty badge absent, active-route highlighting), updated `tests/features/cart/cart-item-quantity-controls.test.tsx` (all 9 success-mutation cases now assert `notify.success` was NOT called), updated `tests/features/homepage/party-heaven-section.test.tsx` (empty products → section renders nothing); docs updated in `docs/dev/ui-conventions.md`, `docs/dev/cart.md`, `docs/dev/architecture.md`, `docs/dev/homepage-section-contract.md`, `docs/ai/task-status.md`, `docs/ai/implemented-features.md`).

Storefront search redesigned as a command dialog completed on 2026-08-20 (scope: replace the separate `/search` page with a shadcn `CommandDialog` that opens from the header on desktop and mobile; implementation: new `CatalogSearchCommandDialog` (`src/features/catalog/components/catalog-search-command-dialog.tsx`) mounted once in `src/app/(storefront)/layout.tsx` and `src/app/page.tsx`, opened via the shared `search-dialog-state` store (`openSearchDialog`/`closeSearchDialog`/`useSearchDialogState`) from the new `SearchDialogTrigger` (desktop labeled button + mobile icon-only button in `app-header.tsx`); the dialog uses `CommandDialog` with `shouldFilter={false}` (results are server-authoritative, cmdk must not client-filter), shows a landing view of "Recent searches" + "Popular searches" groups (popular searches desktop-only via `hidden md:block`), hides the landing groups once a valid query is typed, and renders live debounced (280ms) results from `GET /api/catalog/search` as rows with product image on the left and name + `Rs.` price underneath on the right; recent searches are recorded on Enter submit and on result selection (not every keystroke), with replay/remove/clear-all kept; new curated `popular-searches.ts` seam; the old `/search` page, `CatalogSearchExperience`, `CatalogSearchInput`, and `routes.storefront.search` were removed (robots disallow rules for search-result URLs left harmless); tests: added `tests/features/catalog/catalog-search-command-dialog.test.tsx` (landing groups, mobile-hide wrapper class, landing-hide-on-search, result image/name/price, placeholder fallback, loading, empty, error+retry, Enter records recent, replay recent, remove/clear), `tests/features/catalog/search-dialog-state.test.tsx`, `tests/features/catalog/search-dialog-trigger.test.tsx`, removed the obsolete `catalog-search-experience.test.tsx`, and updated `tests/smoke/storefront-shell.test.ts`; docs updated in `docs/dev/search-architecture.md`, `docs/dev/architecture.md`, `docs/dev/ui-conventions.md`, and this file). Deferred: analytics-driven popular searches (currently curated static list) and per-keystroke server-synced recents remain out of scope.

Currency display convention (Rs.) completed on 2026-08-20 (all user-facing price display now uses the `Rs.` symbol instead of `PKR`; `formatPrice` already defaulted to `Rs.` and this pass removed the remaining user-facing `PKR` usages: admin product/compare-price labels, admin deal-campaign price/compare-at labels, admin settings shipping fee/threshold labels, category SEO FAQ copy, and the notification template price helper renamed `formatPkr` → `formatRs`; admin dashboard and revenue services now report `currency: "Rs."` to match their existing tests; the ISO code `PKR` is intentionally retained for data values only — the Prisma `Currency` enum, DB seed rows, analytics/GA4 + Meta Pixel events, and schema.org `priceCurrency`; docs updated in `README.md`, `docs/dev/checkout.md`, `docs/dev/deployment.md`, `docs/dev/architecture.md`, `docs/dev/domain-model.md`, `docs/dev/setup.md`, `docs/dev/ui-conventions.md`, and `docs/ai/task-status.md`; tests updated in `tests/lib/currency.test.ts` and `tests/smoke/ui-foundation.test.ts`).

Cart drawer + product-card add-to-cart follow-up completed on 2026-08-20 (scope: extend the cart drawer work so storefront cards keep a working add-to-cart affordance everywhere and line items render product thumbnails; implementation: added `CartItemThumbnail` (`src/features/cart/components/cart-item-thumbnail.tsx`) rendered on drawer + cart page line items with `imageUrl`/`imageAlt` populated in `mapCartItem` (variant image first, then product image, ordered by `position`; `null` fallback) and both cart include payloads (`CartIncludePayload` + inline `getCartWithItemsById` include) extended with `images`; homepage `featured-products`/`party-heaven` carousels now render `ProductCardAddToCart` when the product has a slug, and `resolveHomepageFeaturedProducts` backfills from recent published products BEFORE fallback/placeholder items so cards keep add-to-cart; `FeaturedProductItem` gained `inventoryQuantity` (populated in `toFeaturedProductItem` via variant-inventory sum and `toPartyHeavenProductItem` via `card.inventoryQuantity`); the root homepage (`src/app/page.tsx`) now ALSO mounts `<CartDrawer />` because it lives outside the `(storefront)` route group; `CartItemQuantityControls` direct input now normalizes on commit instead of showing inline validation (floats/non-integers keep previous value, negatives/zero clamp to 1, above max clamps down, all committed) and the remove button is icon-only; `ProductCardAddToCart` is icon-only (`size="icon"`, aria-label `Add to cart: {name}`/`Out of stock: {name}`); the PDP `ProductAddToCart` no longer shows a success toast on add (silent, removed `buildAddToCartToastPayload` + `proceedToCheckout` + router usage) while keeping error toasts; `getProductBySlug` resolves the detail `sku` from the default variant first (then master SKU) so the PDP `effectiveSku` matches cart/wishlist line items keyed by variant SKU; tests: added `tests/features/cart/cart-item-thumbnail.test.tsx`, updated `cart-drawer.test.tsx` (thumbnail placeholder assertion), `cart-item-quantity-controls.test.tsx` (clamp-on-commit scenarios), `cart/context.test.ts` + `cart/service.test.ts` (images in fixtures + `imageUrl`/`imageAlt`), `product-add-to-cart.test.tsx` (no-success-toast), and homepage section tests for add-to-cart buttons; docs updated in `docs/dev/cart.md`, `docs/dev/ui-conventions.md`, `docs/dev/architecture.md`, `docs/dev/checkout.md`, and `docs/ai/implemented-features.md`).

Cart drawer + product-card add-to-cart completed on 2026-08-20 (scope: add an `Add to Cart` button to every storefront product card on desktop and mobile — category grid, search results, and related products — while keeping each card a single fully clickable link; clicking the button opens a right-side shadcn `Drawer` on both desktop and mobile containing cart line items with adjust/remove controls, a header with title + close button, and a footer with `View full cart` and `Checkout`; the previous header mini-cart dropdown was removed and both the desktop cart trigger and mobile cart button now open the drawer; implementation: added `vaul` + shadcn `Drawer` (`src/components/ui/drawer.tsx`), a global drawer open/close store (`src/features/cart/cart-drawer-state.ts` with `openCartDrawer`/`closeCartDrawer`/`useCartDrawerState`), `CartDrawer` (`src/features/cart/components/cart-drawer.tsx`, mounted once in the storefront layout, fetches `GET /api/cart`, syncs via `cart:changed`, shows loading/empty/error states, disables checkout on stock issues), `CartDrawerTrigger` (desktop header), updated `MobileCartButton` to open the drawer, and `ProductCardAddToCart` (`src/features/catalog/components/product-card-add-to-cart.tsx`) wired into `ProductGridCard` and `ProductRelatedGrid` as an absolute `z-10` sibling so the card link stays intact; deleted the obsolete `cart-mini-cart.tsx`; tests: added `tests/features/cart/cart-drawer.test.tsx`, `tests/features/cart/cart-drawer-state.test.tsx`, `tests/features/catalog/product-card-add-to-cart.test.tsx`, updated `tests/features/cart/mobile-cart-button.test.tsx`, `tests/features/catalog/product-grid-card.test.tsx`, `tests/features/catalog/product-related-grid.test.tsx`, and `tests/components/layout/app-header.test.tsx`; docs updated in `docs/dev/cart.md`, `docs/dev/ui-conventions.md`, and `docs/dev/architecture.md`.

Homepage hero/blog removal + navbar rework completed on 2026-08-18 (scope: remove the homepage hero section and blog highlights while keeping `/blog` intact, and rework the storefront navbar so categories are shown directly with a trailing `More` dropdown; implementation: removed the `hero-banner` and `blog-highlights` section kinds from the homepage type system (`src/features/homepage/types.ts`), fallback content (`fallback-content.ts` — including the "Everyday essentials, one clear deal at a time." hero), resolver render order, section registry, and service blog hydration, and deleted the now-unused `hero-banner-section.tsx` / `blog-highlights-section.tsx` components plus their test; removed the section kinds from admin homepage management (validation kind enum/schemas/templates, seeding, storefront mapping, and section-form guidance) so legacy DB records of those types are skipped safely; removed the now-unused `homepagePreviewCta` production-visibility guard; navbar: `Home`/`About`/`Blog`/`Contact` moved into the shared `<UserMenu />` dropdown (signed-in and signed-out), desktop navbar now renders live catalog categories directly (capped by `NAVBAR_DIRECT_CATEGORY_LIMIT=6` via new `buildStorefrontNavbarCategoryMenu`) with `More` as the last navbar item holding the remaining categories plus `All Categories` last (linking to `/categories`), and the mobile drawer keeps page links plus the full category list unchanged; tests: added `tests/components/layout/app-header.test.tsx` (direct categories, More last, remaining categories under More, All Categories final + href, no duplicate links), `tests/components/layout/user-menu.test.tsx` (Home/About/Blog/Contact in UserMenu, account/admin/sign-out preserved), extended `tests/components/layout/storefront-category-menu.test.ts` (navbar split, no duplicates, full reachability, all-categories last), and updated homepage/admin-homepage tests for the removed section kinds; docs updated in `docs/dev/architecture.md`, `docs/dev/homepage-section-contract.md`, `docs/dev/content-operations.md`, `docs/dev/ui-conventions.md`, and this file).

Admin related products picker completed on 2026-08-18 (scope: replace the static, server-fetched "Related products" list in the admin product form with a dynamic client-side picker; added debounced search by title/slug and category-aware filtering that defaults to the currently chosen form category, always pins selected items at the top so nothing is accidentally deselected while browsing, and fetches results from a new guarded admin API route so no secrets leak to the browser; implementation: `listAdminRelatedProducts` in `src/features/admin/products/service.ts` now accepts an `AdminRelatedProductsFilter` (query, categoryId, excludeProductId, take, selectedIds) with pinned-item merging, new `GET /api/admin/products/related-search` route protected by `guardRouteHandlerAccess` (admin + catalogWrite), new client component `RelatedProductPicker` in `src/features/admin/products/components/related-product-picker.tsx` (300 ms debounce, loading spinner, empty/error states, checkbox toggling), `AdminProductForm` now wires the picker through `Controller` with the watched `categoryId` and drops the `relatedProducts` prop, and the new/edit admin product pages no longer preload related products; tests: added `tests/features/admin/products/related-product-picker.test.tsx` (search input render, mount fetch with categoryId, debounced query fetch, toggle behavior, pinned selected visibility across query changes, spinner, empty state) and extended `tests/features/admin/products/admin-product-form.test.tsx` with query/category search plus submitted `relatedProductIds` coverage; verified targeted unit tests and full unit test suite).

ProductRelatedGrid image styling refactor, AdminShell background standardization, PNPM workspace configuration, unit tests, and documentation update completed on 2026-08-18 (scope: refactored ProductRelatedGrid component to render product card images directly via `backgroundImage: url(${product.imageUrl})` cover style; standardized AdminShell layout and Sidebar background styling with `bg-background` class for consistent surface rendering; updated `pnpm-workspace.yaml` with `allowBuilds` for `@prisma/client`, `@prisma/engines`, `prisma`, `sharp`, and `unrs-resolver`; added comprehensive unit test suites in `tests/features/catalog/product-related-grid.test.tsx` and `tests/components/layout/admin-shell.test.tsx`; updated developer documentation across `docs/dev/ui-conventions.md`, `docs/dev/setup.md`, `docs/ai/implemented-features.md`, and `docs/ai/task-status.md`; verified all 144 unit test suites and completed production build).

Admin catalog deletion hardening completed on 2026-07-29 (scope: enable admin-side product deletion and resolve variant-removal save failures with minimal architecture-aligned changes; root causes: no product delete action/service/UI existed, and variant update path deleted removed variants without clearing dependent `cart`/`wishlist` rows, causing relational write failures; fix: added `deleteAdminProduct` service + `deleteAdminProductAction` with CSRF/RBAC guards, catalog revalidation, flash messaging, and confirmation UI on both product list and edit surfaces; updated variant upsert flow to clear dependent `WishlistItem`, `CartItem`, and variant-linked `ProductImage` rows before removing deselected variants; category deletion now detaches linked products (`categoryId = null`) before deleting the category so cleanup no longer blocks on attached products; added focused service-level tests for variant-removal dependency cleanup, product deletion dependency cleanup/audit logging, and category detach-on-delete behavior; no unrelated module rewrites and existing contracts preserved). Deferred: optional richer operator UX for pre-delete dependency counts remains out of scope.

Storefront regression + SEO verification and recent-searches rollout completed on 2026-05-09 (scope: audit recent unstaged storefront changes for likely functional and SEO regressions, fix only impactful findings, and implement production-ready recent searches in the existing search UX; findings from audit: targeted regression tests for search/header/auth/category/cart/SEO-sensitive rendering passed, and one likely SEO semantic regression was identified in search page heading structure where the route-level intro no longer declared a primary heading; fix: restored search intro semantics to a proper `h1` through `SectionHeader.titleAs` and reinstated concise descriptive copy while preserving the existing metadata/rendering strategy; recent-searches implementation: added typed local-first utility seam in `src/features/catalog/recent-searches.ts` with query normalization (trim + collapsed whitespace), case-insensitive deduplication, most-recent-first ordering, max-size capping, single-item removal, clear-all, and storage read/write hardening; integrated into `CatalogSearchExperience` with click-to-reuse chips, explicit empty state, storage-failure helper messaging, and no interference with existing search result loading/error/empty states or routing; test coverage: extended `tests/features/catalog/catalog-search-experience.test.tsx` for recent-search render/apply/remove/clear behavior and added `tests/features/catalog/recent-searches.test.ts` for normalization, dedupe, max-size, remove, clear, and malformed storage payload handling; docs updated in `docs/dev/search-architecture.md`, `docs/dev/architecture.md`, and `docs/dev/ui-conventions.md`). Deferred: server-synced cross-device recent searches and popular-query analytics remain intentionally out of scope.

Google Analytics CSP compatibility fix completed on 2026-05-08 (scope: resolve GA script block without broad CSP weakening; root cause: global CSP `script-src` only allowed `'self'` and `'unsafe-inline'`, so the GA4 loader from `https://www.googletagmanager.com/gtag/js` was blocked when `NEXT_PUBLIC_GA_ID` was configured; fix: updated `src/config/security.ts` to add `https://www.googletagmanager.com` to `script-src` only when `NEXT_PUBLIC_GA_ID` is present, keeping analytics script allowlist conditional and host-specific; existing security headers and broader CSP architecture remain intact; added focused CSP generation tests in `tests/config/security.test.ts` for GA enabled/disabled and header output behavior; updated docs in `docs/dev/security.md`, `docs/dev/setup.md`, and `docs/dev/architecture.md` with env and policy guidance). Deferred: nonce/hash-based CSP tightening remains intentionally deferred.

Next Image optimization audit and tuning completed on 2026-05-08 (scope: audit all `next/image` usages in active storefront surfaces and optimize props for performance/layout stability without changing business logic; findings: deprecated `priority` usage remained on key surfaces under Next 16, several responsive card/carousel images lacked accurate `sizes`, and blog card size hints did not match actual grid widths; fix: replaced deprecated `priority` behavior with explicit eager/high-fetch where needed (`homepage hero`, `blog post cover`, PDP primary-gallery image behavior), removed unnecessary explicit quality override on blog cover, and added responsive `sizes` hints for homepage featured category/product/party-heaven cards plus blog listing/highlight cards; preserved existing fallback placeholders, alt semantics, and stable aspect-constrained layouts; added test coverage updates in `tests/features/homepage/featured-categories-section.test.tsx`, `tests/features/homepage/featured-products-section.test.tsx`, `tests/features/homepage/party-heaven-section.test.tsx`, `tests/features/homepage/blog-highlights-section.test.tsx`, and `tests/features/blog/blog-post-card.test.tsx`; docs updated in `docs/dev/ui-conventions.md` and `docs/dev/architecture.md`). Deferred: broader image placeholder-blur pipeline (`blurDataURL` generation for remote CMS images) remains intentionally out of this step.

Admin form page performance optimization completed on 2026-05-08 (scope: diagnose and fix shared slowness across admin pages with forms without changing business logic/security; root causes: list pages were reading edit-level payloads with oversized Prisma selects, and homepage admin routes mounted many heavy client edit forms at first paint; fix: reduced list-query payloads for products/categories/blog to table-relevant fields only in `src/features/admin/products/service.ts`, `src/features/admin/categories/service.ts`, and `src/features/admin/blog/service.ts`; added demand-loaded edit panels for `/admin/homepage/banners`, `/admin/homepage/campaigns`, and `/admin/homepage/sections` via new lazy editor components in `src/features/admin/homepage/components/*-edit-panel.tsx`; preserved existing server actions, validation, RBAC/auth checks, flash messaging, and audit logging behavior; tests updated in `tests/features/admin/products/service.test.ts`, `tests/features/admin/categories/service.test.ts`, and `tests/features/admin/blog/service.test.ts` to guard lightweight select patterns; docs updated in `docs/dev/architecture.md` and `docs/dev/ui-conventions.md`). Deferred: broader admin pagination/search UX and deeper query indexing are intentionally out of this step.

`/categories` performance optimization completed on 2026-05-08 (scope: storefront category index bottleneck reduction without behavior changes; root causes: Party Heaven category count path still did JS-side aggregation across many product rows after reading variant payloads, and category-card images on `/categories` lacked responsive `sizes` hints causing oversized image candidates on smaller screens; fix: optimized `countPublishedPartyHeavenProducts` in `src/server/db/catalog-queries.ts` to run SQL-side scalar counting via a LATERAL selected-variant strategy (default variant first, oldest fallback) with a guarded Prisma fallback path for runtime safety, and added explicit responsive `sizes` in `src/features/catalog/components/category-overview-card.tsx`; tests added in `tests/server/db/catalog-queries.test.ts` (SQL path + fallback path + bigint handling) and updated in `tests/features/catalog/category-overview-card.test.tsx` for image-size hint coverage; docs updated in `docs/dev/architecture.md`). Deferred: broader query/index tuning for non-`/categories` flows remains out of this step.

Prisma P2024 deployment/runtime hardening completed on 2026-05-08 (root cause analysis found no Prisma client singleton duplication; main pressure came from repeated heavyweight catalog queries in hot render paths plus deployment risk from runtime URL misconfiguration; fix: added lightweight cached product-context query (`getPublishedProductContextBySlug`) and lightweight cached Party Heaven count query (`countPublishedPartyHeavenProducts`) in `src/server/db/catalog-queries.ts`; updated catalog service to use these slim paths for related-product context, PDP metadata, and Party Heaven category count surfaces; updated PDP route metadata generation to call `getProductMetadataBySlug` instead of full product detail reads; added deployment/runtime DB strategy safety checks via `getRuntimeDatabaseSafetyCheck` and enforced them in `scripts/guard-deploy-workflow.mjs` so hosted runtime URLs must be pooled (`pgbouncer=true&connection_limit=1`) and distinct from `POSTGRES_URL_NON_POOLING`; added test coverage updates in catalog service/detail mocks, workflow safety checks, and DB client runtime validation; docs updated in `docs/dev/database-access.md`, `docs/dev/setup.md`, and `docs/dev/architecture.md`). Deferred: broad migration of all legacy heavy query call sites outside the catalog runtime hotspots remains out of this step scope.

CartItemQuantityControls on-change quantity validation completed on 2026-05-07 (scope: cart quantity input validation UX only; root cause: direct quantity input only validated on commit and could silently clamp values, causing confusing interactions when entered value exceeded allowed stock; fix: updated `src/features/cart/components/cart-item-quantity-controls.tsx` to validate on every input change and show an inline accessible error message when value is outside the allowed range, with effective max aligned to existing server mutation constraints via `min(availableQuantity, 99)`; invalid values are blocked from blur/Enter commit until corrected; plus/minus max behavior now follows the same effective max; server-side cart update and checkout stock validation remain unchanged and authoritative; tests updated in `tests/features/cart/cart-item-quantity-controls.test.tsx` for valid input, too-high input, too-low input, immediate validation message rendering, message clear-on-correction behavior, and effective-max cap behavior; docs updated in `docs/dev/cart.md` and `docs/dev/checkout.md`).

CartItemQuantityControls direct input editing completed on 2026-05-07 (scope: quantity control UX enhancement; goal: make cart quantity easier to edit directly while preserving existing plus/minus interaction; root cause: users had to use +/- buttons for all quantity changes, no direct input option; fix: replaced static quantity `<span>` with editable `<Input type="number">` field that validates and commits on blur or Enter key, maintaining full plus/minus button functionality alongside direct input; direct input validation enforces whole-number parsing, min 1 and max availableQuantity bounds, with local validation before commit and post-mutation server-truth sync; all mutation paths (input commit, plus, minus, remove) preserve existing optimistic UI, error recovery, and notification behavior; added comprehensive test coverage in `tests/features/cart/cart-item-quantity-controls.test.tsx` for input rendering, plus/minus functionality, direct input interaction, validation, error handling, and accessibility; docs updated in `docs/dev/cart.md` (new section on CartItemQuantityControls direct input + plus/minus behavior) and `docs/dev/ui-conventions.md` (new Numeric Input Patterns subsection); no changes to surrounding cart UX, API contracts, or unrelated components; `dispatchCartChanged()` call restored in runMutation to ensure cart:changed events fire for all mutation paths including direct input commits. Validation: targeted file run for cart features + full `pnpm test` expected to pass).

Failing test audit and fix completed on 2026-05-07 (scope: stabilize current failing test without changing production behavior; failing case: `tests/features/catalog/product-add-to-cart.test.tsx` expected visible text `In cart` in the PDP in-cart controls block; root cause: production UI intentionally renders an icon-only view-cart control with accessible sr-only count label, so the visible-label assertion became stale; fix: updated the test to assert `storefront-view-cart-button` presence plus existing cart-count accessible text assertions, preserving behavioral coverage for `cart:changed` badge synchronization; no runtime code-path changes were required. Validation: targeted file run + full `pnpm test` passed (135 files / 671 tests), and `pnpm build` passed after resolving a known Windows Prisma file-lock prebuild issue by stopping local dev Node/Next processes before rerun. Deferred: none for this slice.)

PDP in-cart quantity-controls switch completed on 2026-05-07 (scope: product page cart interaction only; root cause: PDP always rendered Add to Cart CTA even when the active product variant already existed in cart, forcing users to navigate to cart for quantity adjustments; fix: updated `src/features/catalog/components/product-add-to-cart.tsx` to load/sync cart summary via `/api/cart` + `cart:changed`, detect active cart line by (`productSlug`, `sku`), and conditionally render `CartItemQuantityControls` instead of Add to Cart when quantity is present; added a header-consistent cart icon badge count sourced from `cart.itemCount`; ensured automatic reversion to Add to Cart when the active variant is removed/quantity reaches zero; preserved existing add/update/remove API contracts, stock-aware quantity constraints, guest/auth cart behavior, and mutation event semantics; wired SKU from PDP variant state via `src/features/catalog/components/product-panel.tsx`; expanded coverage in `tests/features/catalog/product-add-to-cart.test.tsx` for add-to-controls transition, variant-aware controls rendering, revert behavior on removal, and badge count updates from `cart:changed` payloads; docs updated in `docs/dev/cart.md` and `docs/dev/architecture.md`. Deferred: none in this slice.)

Storefront category filter/sort end-to-end fix completed on 2026-05-07 (root cause: category listing client components initialized from first payload but did not resync local state when URL query changed, so filters/sort appeared stale and infinite paging could continue with previous query context; fix: added listing-prop resync in `src/features/catalog/components/category-listing-filters.tsx` (desktop/mobile form reset from incoming `listing.filters`) and `src/features/catalog/components/category-infinite-product-grid.tsx` (products/pagination/error reset from incoming listing payload); preserved existing server-side filter/sort parsing and SEO-safe first render contract; expanded test coverage in `tests/features/catalog/filters.test.ts`, `tests/features/catalog/service.test.ts`, `tests/features/catalog/category-listing-filters.test.tsx`, and `tests/features/catalog/category-infinite-product-grid.test.tsx` for query parsing, combined filter+sort behavior, and post-navigation state sync). Deferred: structured `attribute` filtering remains scaffold-only and intentionally unchanged.

Authenticated-user auth-entry-page redirect hardening completed on 2026-05-07 (root cause: authenticated sessions could still render `/auth/sign-in` and `/auth/sign-up`, causing confusing UX and unnecessary auth-form surface exposure; fix: added a shared auth entry-page policy helper in `src/features/auth/auth-page-redirect.ts` and enforced server-side redirects in `src/app/(auth)/auth/sign-in/page.tsx` and `src/app/(auth)/auth/sign-up/page.tsx` so logged-in users are redirected to `/account/profile`; intentionally exempted `/auth/forgot-password`, `/auth/reset-password`, `/auth/verify-email`, and `/auth/error` because they are token/callback recovery surfaces that can still be valid for logged-in sessions; added focused coverage in `tests/features/auth/auth-page-redirect.test.ts` for authenticated redirects, unauthenticated access preservation, and exemption semantics; updated docs in `docs/dev/auth.md` and `docs/dev/architecture.md`. Deferred: none in this slice.)

Header auth stale-state-after-sign-out fix completed on 2026-05-07 (root cause: header auth controls rely on `useSession` from `SessionProvider`, but shared logout controls primarily submitted a server action (`signOutAction`) that cleared cookies and redirected without triggering Auth.js client session synchronization, so client-cached session UI could remain authenticated until a full reload/refetch event; fix: introduced `prepareSignOutAction` in `src/features/auth/actions/sign-out.ts` for trusted-origin validation + guest-cart token rotation, then updated `SignOutButton` to use a client-enhanced submit path that calls `prepareSignOutAction` followed by `signOut()` from `next-auth/react` with the existing home redirect so header/session UI updates immediately on both desktop and mobile surfaces; preserved progressive enhancement by keeping `signOutAction` as fallback and preserving redirect behavior; added focused tests in `tests/features/auth/sign-out-button.test.tsx` for client sign-out path and server fallback, and extended `tests/features/auth/sign-out-action.test.ts` for `prepareSignOutAction`; updated developer docs in `docs/dev/auth.md` and `docs/dev/architecture.md`. Deferred: none in this slice.)

Homepage campaign spotlight DB-backed pricing completed on 2026-05-07 (root cause: campaign spotlight mapper in `src/features/admin/homepage/service.ts` still used an inline static fallback price (`999`) when linked product variant pricing was unavailable, creating drift from real catalog pricing; fix: introduced a dedicated DB-first pricing resolver for campaign spotlights that reads linked product variant `price`/`compareAtPrice` from the existing campaign-product join fetch, applies the same compare-at rule used in catalog/homepage product flows (show discount only when compare-at is greater than price), and removes inline static mapper defaults; fallback behavior now derives from the canonical homepage fallback deal-spotlight contract when linked variant pricing is missing, preserving stable rendering; added focused coverage in `tests/features/homepage/service.test.ts` for DB-backed campaign price selection, missing-pricing fallback, and compare-at normalization, plus rendering assertion in `tests/features/homepage/deal-spotlight-section.test.tsx` for storefront price formatting consistency; docs updated in `docs/dev/architecture.md` and `docs/dev/homepage-section-contract.md`. Deferred: none in this slice.)

Homepage blog-card clickability + image rendering update completed on 2026-05-05 (scope: homepage `blog-highlights` storefront presentation only; fix: updated `src/features/homepage/components/blog-highlights-section.tsx` so each blog card is a full-card canonical `Link` to the detail page, eliminating inner-link-only click targets and nested interactive risk; added image-first card rendering using hydrated blog cover-image metadata with a clean visual fallback state when image data is missing; preserved existing empty-state behavior and blog index CTA; extended homepage blog highlight contract with optional image fields in `src/features/homepage/types.ts` and hydration mapping in `src/features/homepage/service.ts`; added focused UI coverage in `tests/features/homepage/blog-highlights-section.test.tsx` for full-card link behavior, image render/fallback, and empty-state sanity; updated homepage service hydration assertion in `tests/features/homepage/service.test.ts` to include image metadata mapping. Deferred: none in this slice.)

Homepage section add/delete CRUD hardening completed on 2026-05-05 (root cause: resolver composition treated any enabled primary CMS section set as complete, so adding one primary section could suppress other baseline homepage sections unless full seeding had already happened; in parallel, `/admin/homepage/sections` only rendered delete controls for `deal-spotlight`, leaving other section types without a UI removal path; fix: updated `src/features/homepage/resolver.ts` to compose homepage content additively by section kind so enabled CMS sections are preserved while fallback fills only unconfigured kinds, with configured-but-disabled kinds respected as intentionally disabled and deal-spotlight dedupe preserved; updated `/admin/homepage/sections` to expose section deletion for all section types and generalized delete confirmation copy in `src/features/admin/homepage/components/delete-admin-homepage-section-button.tsx`; added/updated coverage in `tests/features/homepage/section-rendering.test.ts` for single-section add stability and multi-type composition, plus `tests/features/admin/homepage/service.test.ts` for generalized section delete behavior; updated developer docs for section contract and architecture composition/delete rules. Deferred: none in this slice.)

Homepage spotlight deal image + target URL support completed on 2026-05-05 (root cause: campaign-generated spotlight overlays from `DealCampaign` lacked explicit image and destination-link fields, so storefront CTAs could resolve to generic fallback routes and spotlight visuals could be incomplete; fix: extended `DealCampaign` data model with optional `targetHref`, `imageUrl`, and `imageAlt` fields and migration, added admin campaign validation/action/form support with safe URL constraints and required alt-text when image is set, and updated storefront mapping in `src/features/admin/homepage/service.ts` so campaign spotlights prefer explicit target/image fields while gracefully falling back to linked campaign product URL/image when missing; strengthened spotlight CTA rendering in `src/features/homepage/components/deal-spotlight-section.tsx` for intentional safe-link behavior (internal route links, external `noopener noreferrer`, invalid-link non-clickable fallback); added coverage in `tests/features/admin/homepage/validation.test.ts`, `tests/features/homepage/service.test.ts`, and `tests/features/homepage/deal-spotlight-section.test.tsx`; updated developer docs for contract and architecture. Deferred operational follow-up: apply the new migration (`20260505123000_deal_campaign_spotlight_fields`) to each runtime database before relying on campaign image/target fields in live data to avoid Prisma `P2022` missing-column errors during admin homepage reads.)

Homepage spotlight deal deletion flow completed on 2026-05-05 (root cause: spotlight-producing admin records could be created/updated but lacked removal paths for both standalone `deal-spotlight` sections and campaign-generated deal overlays; fix: added transactional delete services in `src/features/admin/homepage/service.ts` for `deleteAdminHomepageSection` (`homepage.section.deleted`) and `deleteAdminDealCampaign` (`homepage.campaign.deleted`) with not-found safeguards and audit logging, exposed CSRF/RBAC-protected server actions in `src/features/admin/homepage/actions.ts`, and added destructive-confirmation UX buttons in `/admin/homepage/sections` (spotlight section cards only) and `/admin/homepage/campaigns`; storefront stability confirmed by regression coverage update in `tests/features/homepage/service.test.ts` asserting fallback deal spotlight rendering when no admin spotlight sources remain; admin deletion behavior covered in `tests/features/admin/homepage/service.test.ts` for success + not-found flows).

Homepage spotlight deal section-composition fix completed on 2026-05-04 (root cause: `deal-spotlight` sections created directly through `/admin/homepage/sections` (not via the campaign flow) were classified as "primary" homepage sections in `src/features/homepage/resolver.ts`; when an admin added a standalone spotlight as the only active CMS section, `composeSectionsWithFallback` returned only the CMS sections without merging fallback primary content, so the storefront rendered only header, footer, and the deal block; fix: `deal-spotlight` is now always classified as an overlay section — equivalent to `announcement-bar` — regardless of its id prefix or creation path; deduplication logic was updated to use `hasDealSpotlightSection` (any deal-spotlight present in CMS) instead of only checking for `campaign-` prefix, so fallback deal-spotlight is still suppressed when a CMS-provided one exists; regression test added in `tests/features/homepage/service.test.ts`; developer contract and architecture docs updated).

Homepage admin banner deletion completed on 2026-05-04 (audit confirmed banner create/update existed but delete path was missing in admin; fix: added `deleteAdminBanner` in `src/features/admin/homepage/service.ts` with transactional not-found handling and audit logging (`homepage.banner.deleted`), exposed `deleteAdminBannerAction` in `src/features/admin/homepage/actions.ts` with CSRF + RBAC guardrails, flash messaging, and homepage/admin path revalidation, and added confirmation UX through `DeleteAdminBannerButton` in `/admin/homepage/banners`; updated activity title mapping for deleted banner actions; added focused regression coverage in `tests/features/admin/homepage/service.test.ts` for successful delete + missing-banner behavior, and `tests/features/homepage/service.test.ts` for storefront fallback stability when no banners/admin homepage content remain).

Homepage composition follow-up fix completed on 2026-05-04 (remaining root cause: active campaign-generated deal spotlight sections (`id` prefixed with `campaign-`) were still classified as primary CMS content, so fallback homepage sections could still be suppressed when banners/campaign overlays existed without primary admin-managed homepage sections; fix: updated resolver composition in `src/features/homepage/resolver.ts` so both announcement bars and campaign-generated deal overlays are additive-only and cannot suppress baseline sections; when campaign overlay is present, fallback `deal-spotlight` is omitted to prevent duplicate deal blocks; added regressions in `tests/features/homepage/section-rendering.test.ts` and `tests/features/homepage/service.test.ts` for campaign-only and banner+campaign overlay composition behavior; docs updated in homepage contract and architecture notes).

Homepage banner-only composition regression fix completed on 2026-05-04 (root cause: homepage resolver treated any non-empty admin payload as complete CMS composition; when the only active admin content was banner-derived `announcement-bar` sections, fallback primary sections were suppressed and storefront collapsed to header/footer + banner; fix: updated `src/features/homepage/resolver.ts` to treat `announcement-bar` as additive and merge fallback primary sections when announcements are the only CMS sections; hardened banner mapping in `src/features/admin/homepage/service.ts` to skip empty-title banners and strip invalid hrefs for legacy/incomplete data safety; added focused regressions in `tests/features/homepage/section-rendering.test.ts` and `tests/features/homepage/service.test.ts` for banner coexistence and malformed banner resilience; updated homepage and architecture docs to capture the composition contract).

Homepage hero-banner and deal-spotlight admin verification/hardening completed on 2026-05-04 (audit confirmed both were already manageable through `/admin/homepage/sections` and rendered on storefront via the existing section contract, but media support was incomplete for production authoring; fix: added optional typed `image` payload support for both section types in admin homepage validation, section templates, storefront type contracts, and admin-to-storefront section mapping; added safe image URL constraints aligned with configured storefront hosts; updated hero/deal storefront section components to render optional media with graceful fallback when absent; added focused validation and homepage service regression coverage for media payload acceptance/rejection and fallback behavior when admin records are inactive; updated developer docs for section contract, architecture, and content operations).

Homepage featured-products most-sold ranking completed on 2026-05-04 (root cause: homepage featured products still depended on stored/manual section arrays instead of order history; fix: added `src/features/homepage/featured-products.ts` and wired `src/features/homepage/service.ts` to hydrate the `featured-products` homepage section from live order-item sales data using `SUM(OrderItem.quantity)` grouped by `productId` for parent orders in `CONFIRMED`, `PACKED`, `SHIPPED`, or `DELIVERED` state; published product/category visibility is enforced by re-resolving ranked ids through the storefront catalog query layer; sparse sales data now falls back first to stored `featured-products.content.products` values and then to recent published catalog products so the homepage still shows 4 cards; updated homepage fallback seed copy/data and added focused homepage service coverage for ranking order, publish filtering, and sparse-data fallback behavior).

Homepage featured categories image-shape verification and fix completed on 2026-05-04 (audit confirmed the live catalog hydration path already provided normalized `cardImageUrl`, but legacy/manual homepage category payloads could still carry `imageUrl` while `src/features/homepage/components/featured-categories-section.tsx` rendered only `cardImageUrl`; fix: updated homepage category normalization and admin homepage content validation to accept legacy `imageUrl` input and canonicalize it into `cardImageUrl`, preserving the current render contract and existing fallback placeholder behavior when no safe image URL exists; added focused regression coverage for fallback-section prop shaping in `tests/features/homepage/service.test.ts` plus image-present and fallback rendering behavior in `tests/features/homepage/featured-categories-section.test.tsx`; updated developer architecture notes for the canonical homepage category image contract).

Homepage featured categories DB-connection and type-consistency pass completed on 2026-05-04 (audit confirmed homepage sections were DB-backed at the section-shell level, but `featured-categories.content.categories` was still storefront-primary JSON instead of live catalog data; fix: added runtime hydration in `src/features/homepage/service.ts` so featured categories now resolve from Prisma-backed catalog categories via `getCatalogCategories()`, exclude the virtual `party-heaven` category to avoid duplicate homepage promotion, and preserve stored/manual arrays as a safe fallback when the catalog read fails or returns no publishable categories; normalized the shared homepage category item contract to the same `name`/media field shape used by storefront catalog category cards; updated admin homepage validation to accept legacy `title` payloads while transforming them into the normalized shared shape; added homepage service coverage for DB hydration and fallback normalization plus rendering coverage for the updated category contract; docs updated for homepage/service architecture and section contract).

Form reset-after-success audit and final fix pass completed on 2026-05-04 (audited remaining form components for stale-values-after-success behavior and standardized only expected reset paths; confirmed review form was already aligned with shared reset conventions; fixed remaining auth flows that stay mounted after success by adding success-keyed `form.reset()` behavior in `src/features/auth/components/sign-up-form.tsx` and `src/features/auth/components/reset-password-form.tsx`; preserved existing submit payload contracts, success messaging, disable states, redirect behavior, and error handling; added focused regression coverage in `tests/features/auth/forms.test.tsx` for sign-up and reset-password reset-on-success behavior; intentionally left redirect-first admin CRUD/checkout flows and filter forms unchanged because they either unmount on navigation or should retain user-entered state).

Mobile unwanted-zoom prevention pass completed on 2026-05-04 (audit confirmed root Next viewport config was already standards-compliant and intentionally did not disable accessibility zoom; remaining unwanted zoom risk came from mobile interaction defaults: accidental double-tap zoom on interactive controls and iOS native-control focus zoom for controls rendered below 16px outside shared input primitives; fix: added global mobile interaction guardrails in `src/app/globals.css` with `touch-action: manipulation` on interactive controls plus a WebKit touch-device rule enforcing `font-size: 16px` for native mobile form controls; pinch zoom remains enabled because `maximumScale`/`userScalable` were intentionally unchanged; added regression coverage in `tests/smoke/ui-foundation.test.ts`; updated developer UI conventions with platform tradeoff guidance).

Storefront search result card image mapping fix completed on 2026-05-04 (root cause: DB-backed search result shaping omitted `imageUrl`, so `ProductGridCard` always rendered placeholder gradients in search results even when product images existed; fix: updated `src/features/catalog/search-adapter.ts` to derive the first valid normalized image URL using the shared catalog URL normalizer and include it in `CatalogProductCard` only when safe/renderable; preserved existing fallback behavior by omitting `imageUrl` for missing/invalid/unsafe URLs so placeholder media remains active; added mapping regression tests in `tests/features/catalog/search-service.test.ts` and UI rendering coverage in `tests/features/catalog/catalog-search-experience.test.tsx` to verify both image-present and fallback paths).

Homepage/card type-safety and regression hardening completed on 2026-05-04 (aligned homepage section item contracts with new image-first UI additions by introducing optional `slug` and media fields; updated Party Heaven hydration mapping to include optional card media data; removed accidental debug logging from catalog and homepage paths; fixed unsafe/null badge text rendering in product cards; updated homepage card tests to match full-card link behavior and current visible content; preserved backward compatibility by keeping media fields optional and retaining empty/placeholder rendering when media is unavailable).

Vercel build P2024 connection-pool exhaustion fix completed on 2026-05-01 (root cause: during `next build`, Next.js pre-renders many pages concurrently — each including `<AppHeader />` which calls `getCatalogCategories()` → two Prisma queries; with a Vercel pool limit of 3 connections and 10 s timeout, concurrent static renders exhausted the pool causing P2024 errors; fix: wrapped `listPublishedCategories` and `listAllPublishedProducts` in `src/server/db/catalog-queries.ts` with `unstable_cache` (TTL 900 s, tags `catalog:categories` / `catalog:products`) so all concurrent renders share a single DB round-trip during the build window; added `CATALOG_CACHE_TAGS` constants export so admin actions can call `revalidateTag` for on-demand invalidation; updated `src/features/admin/categories/actions.ts` and `src/features/admin/products/actions.ts` to call `revalidateTag(..., "max")` on catalog mutations in addition to existing `revalidatePath` calls; tests: all 129 test files / 609 tests pass; build completes without errors).

Follow-up Vercel build hardening completed on 2026-05-01 (remaining prerender-time P2024 spikes were traced to repeated slug-level `findFirst` lookups in product/category routes; `getPublishedProductBySlug` and `getPublishedCategoryBySlug` in `src/server/db/catalog-queries.ts` now use `unstable_cache` per slug with existing catalog tags and a single retry on Prisma `P2024` to absorb transient connection-pool contention during concurrent static generation).

SEO rendering strategy correction for content pages completed on 2026-05-01 (audited storefront rendering behavior and identified request-time auth in shared `AppHeader` as a major dynamic-rendering trigger; moved header auth controls to a client session island so SEO pages are not forced dynamic by shared chrome; standardized SEO content ISR to `900s` across content routes; updated static-param generation for blog/category/product routes to typed shared helpers in `src/features/rendering/seo-content-rendering.ts`; moved PDP review eligibility personalization into a client/API island (`ProductReviewComposer` + `GET /api/reviews/composer-context`) so product HTML remains cacheable while user-specific review controls remain correct; expanded admin on-demand revalidation for catalog mutations to include `/categories/[slug]` and `/categories/[slug]/[productSlug]` along with listing routes; added rendering-helper tests for ISR constants and static-param mapping behavior; preserved metadata, JSON-LD, and existing loading/empty/error state behavior; intentionally deferred broad non-SEO route rendering refactors outside this scope).

Semantic HTML and crawler-friendly markup improvement pass completed on 2026-05-01 (audited homepage-adjacent and key storefront SEO surfaces: category index, category listing, product related grids, blog listing/detail, and shared navigation/footer/cards; introduced explicit primary heading support in shared `SectionHeader` and applied `h1` semantics to key route pages; upgraded card-grid containers to semantic list markup (`ul`/`li`) while preserving current styling and behavior; improved shared header/footer structure with cleaner nav/list landmarks; refined blog cards to use one canonical full-card link plus `<time datetime>` publication metadata; preserved existing loading, empty, and error-state behavior; added focused markup tests for section-heading semantics, blog card canonical/time markup, and category/product card semantic wrappers; intentionally deferred a full app-wide semantic sweep outside the requested SEO-critical storefront scope and deferred automated end-to-end crawler snapshot assertions to a dedicated SEO testing slice).

Storefront category infinite-scroll listing completed on 2026-05-01 (replaced button-based pagination on `/categories/[slug]` with a React infinite-scroll flow using `react-infinite-scroll-component`; preserved SEO-safe first render by server-rendering the first 6 products through existing category route data loading; added a dedicated paged API route `GET /api/catalog/categories/[slug]/products` for progressive loading with user-safe error responses; retained existing filter/sort/category correctness by reusing shared query parsing/building contracts; added clear product-count progress messaging, loading state, retry-on-error behavior, and explicit end-of-list state so users can tell when more products exist and when the list is complete; added focused tests for initial page size defaulting, query-param pagination helper behavior, infinite append behavior, and end-of-list rendering). 

Storefront category/product card clickability audit standardization completed on 2026-05-01 (audited all storefront category and product card surfaces and standardized click behavior so cards are navigable through a single semantic wrapping link; verified existing full-card link behavior in catalog category cards, catalog product grid cards, related product cards, and homepage featured category cards; upgraded homepage featured-product and party-heaven product cards from text-only CTA links to full-card links while preserving card layout/styling and existing empty/loading semantics; avoided nested interactive element patterns by keeping one anchor per card; added regression tests for full-card link behavior in homepage and catalog card suites; updated developer UI conventions to codify the single-link-per-card rule for accessibility and crawler-friendly markup).

Category card background image support completed on 2026-05-01 (added a dedicated nullable `Category.cardImageUrl` persistence field and migration; wired admin category create/edit payload parsing, validation, form controls, and Prisma writes so product/catalog admins can add/update/remove category card image URLs through the existing shared upload input pattern with `purpose: category`; updated storefront category query mapping and category-card rendering to display category-specific background media when available; preserved safe visual fallback rendering when no image exists; added focused tests for admin category image validation/save payload behavior and storefront category-card image/fallback rendering; updated developer and AI task docs for the new contract).

Catalog product-card media image-first update completed on 2026-05-01 (updated storefront catalog card media area to render product images when a valid `imageUrl` is available; retained deterministic gradient placeholder behavior when image data is missing/invalid; added load-error fallback in card media so broken image responses gracefully switch to the existing placeholder treatment instead of displaying broken media; preserved media aspect ratio and card layout stability; introduced URL normalization guard for catalog image URLs to accept only root-relative and absolute HTTP(S) values; added focused catalog card rendering tests for image-present, image-missing fallback, and load-error fallback scenarios; updated UI conventions documentation with the new image-first + graceful-fallback contract).

Add-to-cart success sonner CTA theme alignment completed on 2026-05-01 (fixed mobile add-to-cart toast action label typo to `Proceed to Checkout` to match existing UX/tests; updated shared `AppToaster` Sonner classNames so action/cancel toast buttons now use app `buttonVariants` styling instead of Sonner's default black action button, keeping CTA visuals consistent in the fixed light theme; preserved toast duration, mobile-only CTA behavior, and checkout routing callback contract; added focused provider tests in `tests/components/providers/app-toaster.test.tsx`).

Homepage featured categories large-screen density adjustment completed on 2026-05-01 (updated featured categories carousel display configuration so large screens now render 4 visible cards instead of 6 to reduce wrapped category text and improve readability; mobile/smaller breakpoint behavior, carousel controls, max-item capping, and View All logic remain unchanged; change was isolated to featured-categories config usage without affecting featured-products or party-heaven carousel density; updated focused homepage carousel tests and UI conventions documentation).

Cart add-to-cart synchronization hardening + failing test stabilization completed on 2026-05-01 (hardened storefront add-to-cart success flow to emit a no-detail `cart:changed` event fallback when API success responses unexpectedly omit cart detail, allowing subscribers to re-fetch instead of rendering stale or incorrect cart state; added regression coverage in `tests/features/catalog/product-add-to-cart.test.tsx`; fixed currently failing test suites caused by sidebar/navigation API drift by restoring the `About` storefront nav item in `src/config/site.ts`, aligning sidebar primitive tests with current `SidebarProvider`/accessibility labels and mobile detection behavior, and wrapping admin sidebar nav tests in `SidebarProvider`; no cart business rules or API contracts were changed).

Review form shared dynamic-form migration completed on 2026-04-30 (migrated `CustomerReviewForm` to the shared shadcn form stack using `useAppForm`, `DynamicForm`, and `useServerActionSubmit`; preserved existing review field names, payload shape, validation constraints, moderation compatibility, and mobile collapse UX; standardized successful-submit reset behavior through shared submit-success callback flow while keeping redirect behavior safe; updated review-form tests to cover validation blocking, payload integration, and reset-after-success behavior; updated developer architecture/UI conventions docs for the new pattern).

Theme default mode update completed on 2026-04-30 (updated `ThemeProvider` default initialization from `system` to `light` so first-time visits start in light mode; preserved theme switching and dark/system support through existing `next-themes` configuration; added focused provider behavior tests in `tests/components/providers/theme-provider.test.tsx`; updated developer UI conventions to document light-as-default behavior). Superseded on 2026-08-31 by the light-only theme conversion — `next-themes`, `ThemeProvider`, and `ThemeToggle` were removed and the app now always renders light.

Local/dev demo catalog population script completed on 2026-04-30 (added deterministic dataset helper in `prisma/dev-catalog-data.js`; added guarded local/dev-only population runner in `prisma/populate-dev-catalog.js`; wired command `pnpm prisma:seed:dev-catalog`; populated the requested category set with deterministic 4-8 product counts per category, category/product SEO metadata, dev-safe image URLs, and mixed pricing that intentionally includes products at or below `Rs. 280` to exercise virtual Party Heaven category behavior; added generator/data-shape tests in `tests/prisma/dev-catalog-data.test.ts`; updated local setup and domain model docs).

Production placeholder/development artifact guard pass completed on 2026-04-30 (added centralized visibility rules in `src/config/production-visibility.ts`; production now suppresses preview/incomplete storefront artifacts while keeping them available in development and test; `/preview` and placeholder-only `/return-policy` now resolve to not-found in production; homepage fallback indicator/preview CTA are production-guarded; footer preview link and newsletter placeholder artifacts are production-guarded; not-found admin placeholder action and about-page interim narrative note are production-guarded; functional homepage empty states remain visible but now use neutral non-placeholder copy; added focused env/guard tests in `tests/config/production-visibility.test.ts`; architecture and UI convention docs updated with guard policy).

Mobile review form collapsible UX completed on 2026-04-30 (the add-review form on the product page is now collapsed by default on mobile and expanded by default on desktop; a toggle button in the section header with `aria-expanded`/`aria-controls` semantics allows users to expand or collapse the form; collapse-on-success is handled automatically — the server action redirects after submit, re-mounting the component so the mobile default-collapsed state is restored without extra prop wiring; desktop layout and form behavior are unchanged; `useIsMobile()` drives the auto-collapse via a guarded `useEffect` with a `hasAutoCollapsedRef` to prevent clobbering user overrides; tests added in `tests/features/reviews/customer-review-form.test.tsx` covering default mobile collapse, expand/collapse toggle, re-mount restore, desktop expand, and locked state rendering; `docs/dev/ui-conventions.md` updated with the Mobile Collapsible Form Sections pattern).

Mobile category filter/sort sheet UX completed on 2026-04-30 (category listing now uses a mobile-only shadcn `Sheet` trigger/panel for filter and sort controls with explicit open/close actions, while desktop preserves the existing persistent sidebar card; filter/sort schema and URL behavior are unchanged and still route through the same query-string builder; added coverage in `tests/features/catalog/category-listing-filters.test.tsx` for responsive render scoping, sheet toggle behavior, and URL state preservation on apply).

Global cart item count state + mobile cart badge synchronization completed on 2026-04-30 (added a dedicated client cart-count state seam in `src/features/cart/cart-count-state.ts` with first-subscriber API bootstrap and `cart:changed` event synchronization, reused existing cart mutation/event behavior without changing business logic, and replaced the mobile header cart icon with `MobileCartButton` so mobile now reflects total item count; added tests for count synchronization, event-driven refresh behavior, and mobile cart count rendering/accessibility text).

Mobile add-to-cart sonner UX improvement completed on 2026-04-30 (storefront add-to-cart success toast now uses a dedicated payload builder in `src/features/catalog/lib/add-to-cart-toast.ts`; toast display duration increased to `5000ms`; mobile viewports receive a `Proceed to Checkout` toast action that navigates to `/checkout`; desktop behavior remains non-actionable while sharing the safer longer visibility window; add-to-cart success flow, cart event dispatch, and error handling behavior preserved; targeted tests added for payload builder behavior and mobile/desktop CTA logic in `tests/features/catalog/add-to-cart-toast.test.ts` and `tests/features/catalog/product-add-to-cart.test.tsx`; UI conventions doc updated with this notification pattern).

Homepage carousel standardization completed on 2026-04-30 (all three homepage sections that surface categories or products — `featured-categories`, `featured-products`, `party-heaven` — now use a single shared carousel pattern; shared config in `homepage-carousel-config.ts` defines `HOMEPAGE_CAROUSEL_MAX_ITEMS=8`, a 1–6 card responsive basis class ladder, and `align:"start"` options; nav buttons hide on mobile and disappear when scroll is exhausted via `disabled:hidden`; View All link shown when items exceed the cap or an explicit `viewAllHref` is provided by the section payload; `party-heaven` CTA always shown; empty states preserved; new tests added for `FeaturedProductsSectionBlock` and `PartyHeavenSectionBlock`; updated `featured-categories-section.test.tsx` for new config and capping behavior; Party Heaven service fetch limit bumped from 6 to 8; docs updated in `ui-conventions.md`, `homepage-section-contract.md`, and `task-status.md`).

Storefront category card clickability improvement completed on 2026-04-30 (homepage featured category cards now use full-card `Link` wrappers so the whole card is keyboard and pointer clickable; card-level focus-visible rings are preserved; nested interactive semantics were avoided by removing inline nested link-only CTA behavior; targeted homepage section tests were updated to assert title-to-link containment and expected href mapping).

Party Heaven homepage section completed on 2026-04-30 (new `party-heaven` section kind inserted between Featured Categories and Featured Products; products hydrated at runtime from live catalog via `getCatalogCategoryListing`; section renders a graceful empty/placeholder state when no qualifying products exist; `PartyHeavenSection` type added to the homepage type system; section registered in the render component map, resolver order, admin validation, and admin DB mapper; fallback content seeded with a default `party-heaven` shell; tests updated/added for hydration and graceful failure; docs updated in `homepage-section-contract.md` and `task-status.md`).

Party Heaven default category behavior completed on 2026-04-29 (implemented as a virtual/system storefront category rather than a persisted DB category; membership is now derived from published products priced at `<= Rs. 280`; products continue to belong to their original category relations; Party Heaven is now consistently available in storefront navigation and category surfaces via the reserved `party-heaven` slug; added listing/menu test coverage for derived inclusion and fallback routing).

Header category navigation dropdown update completed on 2026-04-29 (desktop header now uses a `Party Heaven` dropdown menu with deterministic ordering: `Party Heaven` first, published categories next, and `All Categories` last; mobile drawer now mirrors the same category ordering pattern in a grouped section; non-fatal category load failures now preserve header rendering with user-safe fallback messaging; added ordering tests in `tests/components/layout/storefront-category-menu.test.ts`).

Form success behavior audit and fix completed on 2026-04-29 (audited all forms for missing reset/close-on-success; fixed `ForgotPasswordForm` to call `form.reset()` and disable the submit button after a successful dispatch; added `resetOnSuccess` prop to `DynamicForm`/`SchemaForm` for inline forms that stay mounted after success; documented the four-pattern standard in `docs/dev/ui-conventions.md`; added tests covering forgot-password reset, button disable, and `DynamicForm.resetOnSuccess`). All admin forms, checkout, and customer-review intentionally left unchanged — they redirect via server action and do not need client-side reset.

Theme and visual quality pass completed on 2026-04-28 (global light/dark tokens enforced the requested palette, shared form/table/dialog/nav surfaces were refined through semantic classes, and smoke tests included palette/token sanity coverage; the dark token set was later removed by the 2026-08-31 light-only theme conversion).

Admin image upload expansion completed on 2026-04-28 (shared upload-enabled image fields now cover product image rows plus existing banner/blog/SEO flows, while preserving existing URL-based payload/data contracts).

Deployment Prisma safety hardening completed on 2026-04-28 (deploy-path failure reproduced at `prisma migrate deploy` with `P3009`; added deploy workflow guard, hosted pooled-vs-direct migration URL safety checks, and explicit migration incident recovery commands/docs).

Admin low-inventory visibility fix completed on 2026-04-28 (shared threshold-aware low-stock query now powers both dashboard and inventory views, with per-item safety-stock override and global store-threshold fallback).

Homepage featured categories carousel update completed on 2026-04-28 (section now uses shadcn-compatible carousel rendering with responsive card density by viewport and a resilient empty-state fallback when no categories are configured).

Cart session separation hardening completed on 2026-04-28 (guest vs authenticated context isolation, explicit merge guardrails, sign-out context reset).

Admin sidebar standardization completed on 2026-04-28 (custom admin sidebar shell migrated to shared shadcn-style sidebar primitives with consistent desktop collapse + mobile drawer behavior, while preserving existing permission-aware navigation filtering and routes).

Mobile interaction quality pass completed on 2026-04-29 (added typed root viewport config with standards-compliant defaults, avoided accessibility-hostile zoom lockouts, upgraded shared mobile text-entry sizing to prevent common iOS focus zoom, and slightly increased mobile admin sidebar control density for clearer touch ergonomics while preserving desktop behavior).

Scope note for this slice: storefront category filter side panels remain unchanged because they are feature-level content filters (not app-shell navigation); migration focused on app-level admin navigation sidebars only.

Related products reliability pass completed on 2026-04-28 (explicit admin-curated handling hardened, same-category fallback behavior stabilized, self-exclusion enforced by slug/id, and PDP related-section empty-state made explicit when no valid recommendations exist).

## Purpose of this File

This file is now intentionally concise. Detailed implementation state is split into focused docs:

- `docs/ai/implemented-features.md`
- `docs/ai/open-tasks.md`
- `docs/ai/architecture-decisions.md`
- `docs/ai/testing-status.md`

## Current State Snapshot

- Product baseline is production-minded and broad: storefront, admin, auth/security, cart/checkout/orders, reviews, blog, homepage CMS controls, contact, and email-marketing foundations.
- Database-backed catalog and blog flows are active; publish-state visibility is enforced.
- Shared infrastructure for forms, tables, error handling, logging redaction, and RBAC is active and used across major modules.
- Admin/content image uploads now have a single reusable path: guarded route handler, provider abstraction, shared image upload input, and final-URL persistence back into existing form values across product, category/blog SEO, blog cover, and banner forms.
- Test suites (unit/integration/e2e) and build pipelines are in place and routinely used as release gates.
- Cart context resolution now enforces guest/auth isolation in shared browser sessions and prevents authenticated cart token leakage into guest cart resolution.

## Active Deferred Buckets

- Advanced category media variants beyond card background imagery (for example separate hero/campaign assets) and image URLs embedded inside JSON content editors remain intentionally deferred to follow-up slices.
- Online payment providers and webhook processing
- Advanced inventory operations (history filters are the recommended next implementation step)
- Advanced revenue analytics and export workflows
- Activity feed filter/pagination UI controls
- Email-marketing double opt-in and abandoned-cart recovery automation
- Extended admin settings (tax/payment/warehouse policy matrices)
- Optional future enhancement: one-time explicit merge intent cookie/flag (only needed if product wants merge strictly at login event boundaries rather than at merge-enabled cart/checkout resolution points)

## Recommended Next Prompt

Proceed with: Phase 5.1 inventory history filters and adjustment reason taxonomy.

## Continuity Rule

When a feature is added, modified, or deferred:

1. Update `docs/ai/implemented-features.md`.
2. Update `docs/ai/open-tasks.md`.
3. Update `docs/ai/architecture-decisions.md` if design tradeoffs changed.
4. Update `docs/ai/testing-status.md` if test posture changed.
5. Keep this file as a short status checkpoint only.
